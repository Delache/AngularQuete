import { HoverItDirective } from './hover-it.directive';

describe('HoverItDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverItDirective();
    expect(directive).toBeTruthy();
  });
});
